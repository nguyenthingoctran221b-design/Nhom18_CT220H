name: Flutter CI — Build & Publish APK

on:
  push:
    branches: [main, master]
    tags:
      - 'v*.*.*'          # trigger on semver tags, e.g. v1.0.0
  pull_request:
    branches: [main, master]

# ──────────────────────────────────────────────────────────────────────────────
# Concurrency: cancel any in-progress run on the same branch/PR
# ──────────────────────────────────────────────────────────────────────────────
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================================================
  # Job 1 – Analyze & Test
  # ============================================================================
  analyze:
    name: Analyze & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.x'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        working-directory: UD_dat_mon_an
        run: flutter pub get

      - name: Run analysis
        working-directory: UD_dat_mon_an
        run: flutter analyze --no-fatal-infos

      - name: Run tests
        working-directory: UD_dat_mon_an
        run: flutter test --coverage || true

  # ============================================================================
  # Job 2 – Build APK (debug on PR, release on push to main/tag)
  # ============================================================================
  build:
    name: Build APK
    runs-on: ubuntu-latest
    needs: analyze           # only build if analysis passes

    env:
      FLUTTER_VERSION: '3.29.x'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── Set up Flutter ──────────────────────────────────────────────────────
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: Set up Java 17 (required by Gradle)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        working-directory: UD_dat_mon_an
        run: flutter pub get

      # ── Debug APK (PR only) ─────────────────────────────────────────────────
      - name: Build debug APK
        if: github.event_name == 'pull_request'
        working-directory: UD_dat_mon_an
        run: flutter build apk --debug

      - name: Upload debug APK artifact
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: smart-emenu-indochine-debug-${{ github.sha }}
          path: UD_dat_mon_an/build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 7

      # ── Release APK (push to main / tag) ───────────────────────────────────
      - name: Build release APK
        if: github.event_name != 'pull_request'
        working-directory: UD_dat_mon_an
        run: |
          flutter build apk \
            --release \
            --build-name=${{ github.ref_name }} \
            --build-number=${{ github.run_number }}

      - name: Upload release APK artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: smart-emenu-indochine-release-${{ github.sha }}
          path: UD_dat_mon_an/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

  # ============================================================================
  # Job 3 – Publish a GitHub Release (only on semver tags)
  # ============================================================================
  release:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write      # required to create a release

    steps:
      - name: Download release APK
        uses: actions/download-artifact@v4
        with:
          name: smart-emenu-indochine-release-${{ github.sha }}
          path: artifacts/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Smart E-Menu Indochine ${{ github.ref_name }}"
          tag_name: ${{ github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          generate_release_notes: true
          files: artifacts/app-release.apk
